import { WaitlistPage } from "@/templates/waitlist/components/WaitlistPage";

export const metadata = {
    title: "Waitlist | NexStack",
    description: "Join the waitlist for NexStack.",
};

export default function Page() {
    return <WaitlistPage />;
}
