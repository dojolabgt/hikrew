FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# app/ is mounted as a volume in docker-compose.dev.yml
CMD ["python", "-m", "app.main"]
