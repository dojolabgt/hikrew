services:
  frontend:
    build:
      context: ./frontend-app
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    environment:
      - NODE_ENV=${NODE_ENV}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - app-network
    # Dokploy handles port mapping via reverse proxy
    expose:
      - "3000"

  frontend-public:
    build:
      context: ./frontend-public
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_DASHBOARD_URL: ${NEXT_PUBLIC_DASHBOARD_URL}
    environment:
      - NODE_ENV=${NODE_ENV}
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
      - NEXT_PUBLIC_DASHBOARD_URL=${NEXT_PUBLIC_DASHBOARD_URL}
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - app-network
    # Dokploy handles port mapping via reverse proxy
    expose:
      - "3000"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: .env
    environment:
      - PORT=${PORT}
      - DATABASE_HOST=${DATABASE_HOST}
      - DATABASE_PORT=${DATABASE_PORT}
      - DATABASE_USER=${DATABASE_USER}
      - DATABASE_PASSWORD=${DATABASE_PASSWORD}
      - DATABASE_NAME=${DATABASE_NAME}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - FRONTEND_URL=${FRONTEND_URL}
      - NEXT_PUBLIC_DASHBOARD_URL=${NEXT_PUBLIC_DASHBOARD_URL}
      - FRONTEND_PUBLIC_URL=${FRONTEND_PUBLIC_URL}
      - NODE_ENV=${NODE_ENV}
      - STORAGE_TYPE=${STORAGE_TYPE}
      - UPLOAD_MAX_SIZE=${UPLOAD_MAX_SIZE}
      - ALLOWED_IMAGE_TYPES=${ALLOWED_IMAGE_TYPES}
      - SEED_ADMIN_EMAIL=${SEED_ADMIN_EMAIL}
      - SEED_ADMIN_PASSWORD=${SEED_ADMIN_PASSWORD}
      - SEED_FREELANCER_EMAIL=${SEED_FREELANCER_EMAIL}
      - SEED_FREELANCER_PASSWORD=${SEED_FREELANCER_PASSWORD}
      - MAIL_HOST=${MAIL_HOST}
      - MAIL_PORT=${MAIL_PORT}
      - MAIL_USER=${MAIL_USER}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - MAIL_FROM=${MAIL_FROM}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - BLEND_RECURRENTE_PUBLIC_KEY=${BLEND_RECURRENTE_PUBLIC_KEY}
      - BLEND_RECURRENTE_SECRET_KEY=${BLEND_RECURRENTE_SECRET_KEY}
    depends_on:
      - postgres
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped
    networks:
      - app-network
    # Dokploy handles port mapping via reverse proxy
    expose:
      - "4000"

  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network
    # Internal port only
    expose:
      - "5432"

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
  uploads:
